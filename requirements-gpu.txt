# يُثبَّت هذا الملف فقط على خادم يحتوي GPU فعلي (RTX 5090)
# التثبيت يتم عبر setup.sh/restore.sh باستخدام --index-url مطابق لإصدار CUDA المكتشف ديناميكياً
# لا تُثبَّت هذه الحزم مباشرة بأمر pip install -r عادي لتفادي تنزيل حزمة CUDA عامة ضخمة غير مطابقة
torch
torchvision
torchaudio
